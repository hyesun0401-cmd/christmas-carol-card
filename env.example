# Copy values into your shell env or into `.env.local` (which is git-ignored).

# Database (SQLite)
DATABASE_URL="file:./dev.db"

# Spotify (optional)
SPOTIFY_CLIENT_ID="..."
SPOTIFY_CLIENT_SECRET="..."

# Gemini CLI (optional)
GEMINI_API_KEY="..."


